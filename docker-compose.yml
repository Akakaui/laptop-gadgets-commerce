services:
  shop:
    build: .
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 8787
      DATABASE_PATH: /app/data/kora.sqlite
    volumes:
      - kora-data:/app/data

volumes:
  kora-data:
